;이벤트플래그:111:0 리스트
;루트를 만드는 단계가 아닌고로 스토리 진행의 플래그만 있음
;0:시작 전     1:스토리 1화 클리어

;=================================================
;   sub EVENT_111_FLAG_RESET
;=================================================
;   의뢰:フラグ리셋
;-------------------------------------------------
;   MENU화면で「의뢰출현」が出る様、
;   의뢰등록フラグを리셋する
;-------------------------------------------------
@EVENT_111_FLAG_RESET
SIF GETBIT(FLAG:이벤트출현2,111-64)
	CLEARBIT FLAG:이벤트출현2, 111-64

;====================
;조건
;====================
@PLAYABLE_EVENT_111
IF BASE:MASTER:LV > 15 && 이벤트フラグ:111:0 == 0
	RESULTS = [디지몬 MOD] Into the Coulomb
	RETURN 1
ENDIF
RETURN 0


;====================
;内容
;====================
@SHOPCOM_EVENT_111
IF 이벤트フラグ:111:0 == 0
	CALL EVENT_111_1
	RETURN 0
ENDIF
